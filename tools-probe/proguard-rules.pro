# Tools Probe is debug-only and ships without minification. Empty placeholder kept so
# the `release` build type's proguardFiles directive can reference a project-local rules
# file without failing the configuration phase. Add rules here only if a future probe
# variant enables minification.
