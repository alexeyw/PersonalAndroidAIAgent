# Pipeline cookbook

A pipeline is a graph of typed nodes. This page is the reference for those
types — what each one does, what it does with its input, which of its
configuration fields actually change a run — and a set of recipes that wire
them into shapes worth copying.

Where the other pages stop:

- [user-guide.md](user-guide.md) describes the screens: how to open the
  editor, draw a connection, save a pipeline. It is the canonical account of
  how the app behaves.
- [faq.md](faq.md) answers whether a thing exists and where it lives.
- [troubleshooting.md](troubleshooting.md) is for when something is broken.
- [extending.md](extending.md) is for adding a *new* node type in Kotlin.

This page assumes you have the editor open and a pipeline on the canvas. If
you have not got that far, start with
[Pipelines](user-guide.md#pipelines) in the user guide.

## Contents

- [How to read the node reference](#how-to-read-the-node-reference)
- [Node overview](#node-overview)
- [Node reference](#node-reference)
- [Recipes](#recipes)
  - [Routing on intent](#routing-on-intent)
  - [A list of subtasks, worked through one at a time](#a-list-of-subtasks-worked-through-one-at-a-time)
  - [A tool call you approve](#a-tool-call-you-approve)
  - [A run that uses long-term memory](#a-run-that-uses-long-term-memory)
  - [One pipeline calling another](#one-pipeline-calling-another)
  - [What goes wrong most often](#what-goes-wrong-most-often)
- [See also](#see-also)

## How to read the node reference

Everything between here and [Recipes](#recipes) is generated from the Kotlin
sources that define a node — the node type enum, the port factory, the
per-type context defaults, and the configuration data classes. It is
regenerated by `./gradlew :app:generateCookbookDocs` and a mandatory build
check fails when it drifts, so what you read here is what the code does.

Three columns need a word of explanation.

**In / Out** are the node's ports — the dots you drag a connection from and
to. A node with two outbound ports is a fork: which one the run follows is
decided by the node.

**Context on a new node** is which blocks of the run's context the node is
given along with its input. Five blocks exist — `chatHistory`,
`originalTask` (the message that started the run), `nodeInput` (the previous
node's output), `longTermMemory` and `toolResults` — and each node has its
own default, because a small on-device model given everything answers worse
than one given the relevant thing. You can change them per node under
**Input Data** in its configuration sheet.

**Reaches the run** is the column to read before you spend time on a field.
A node's configuration sheet is one screen, but not everything on it is
wired to the engine: some fields are stored with the pipeline and exported
with it while nothing consults them during a run. Those are marked **No**,
with what happens instead. Fields marked **Yes** land on the saved node and
change what happens; **Shapes the graph** means the value decides which
ports the node has, and the run then follows the edges those ports carry.

> **Four prompt fields are not written back.** The `Intent Router`,
> `Decomposition`, `Evaluation` and `Summary` sheets each show a prompt field
> that the editor stores but does not put where the engine reads it. Editing
> one appears to work — it survives closing and reopening the sheet — but the
> node goes on running the prompt it was created with. To give one of these
> nodes a different prompt today, edit the pipeline as JSON (`config.systemPrompt`
> on the node) and import it, or use the browser editor. This is a defect, not
> a design; it is recorded in [Known limitations](faq.md#known-limitations).

## Node overview

<!-- AUTO-GEN:NODE_REFERENCE -->
| Node | What it does | In | Out | Context on a new node |
|---|---|---|---|---|
| **Input**<br>`INPUT` | Where the run starts: your message, a shared page, or the prompt a trigger carries. Exactly one per pipeline. | — | 1 | input forwarded as-is |
| **Output**<br>`OUTPUT` | Where the run ends and the answer reaches you. With no prompt of its own it forwards the previous node's text verbatim; give it one and it re-formats that text with one more model pass. Exactly one per pipeline. | 1 | — | nodeInput \* |
| **LiteRT**<br>`LITE_RT` | One inference step on the model running on the phone. The default answering node, and the one to reach for unless a step genuinely needs a larger model. | 1 | 1 | originalTask, nodeInput |
| **Cloud**<br>`CLOUD` | One inference step against a configured cloud provider. Everything this node is given leaves the device, so it is a node to place deliberately rather than by default. | 1 | 1 | chatHistory, originalTask, nodeInput |
| **Intent Router**<br>`INTENT_ROUTER` | Sorts the incoming text into one of the classes you declare and sends the run down the matching branch — the way to give one pipeline several behaviours. | 1 | one per class | chatHistory, originalTask, nodeInput |
| **If Condition**<br>`IF_CONDITION` | A two-way branch: a yes/no question about the input, or a deterministic check for whether the run carries an image. | 1 | True / False | input forwarded as-is |
| **Clarification**<br>`CLARIFICATION` | Pauses the run to ask you a question and waits for the answer, which then becomes the node's output. The one node that deliberately stops mid-run. | 1 | 1 | originalTask, nodeInput |
| **Tool**<br>`TOOL` | Calls one tool — a built-in, an AppFunction from another app, or one published by an MCP server — and passes the result on. Anything its risk level does not clear waits for your approval. | 1 | 1 | nodeInput |
| **Decomposition**<br>`DECOMPOSITION` | Turns one instruction into a list of subtasks. On its own it only produces the list; pair it with a Queue Processor to work through it. | 1 | 1 | originalTask, nodeInput |
| **Queue Processor**<br>`QUEUE_PROCESSOR` | Walks a list of subtasks one at a time, sending each down the Item branch and taking the Done branch when the list is empty. The only node that can send a run backwards. | 1 | Item / Done | input forwarded as-is |
| **Evaluation**<br>`EVALUATION` | Judges what the previous step produced and answers Pass, Retry or Fail — the node that lets a pipeline have another go instead of handing you a bad answer. | 1 | Pass / Retry† / Fail | originalTask, nodeInput, toolResults |
| **Summary**<br>`SUMMARY` | Condenses what several earlier steps produced into one piece of text, typically just before the Output node at the end of a loop. | 1 | 1 | originalTask, nodeInput, toolResults |
| **Pipeline**<br>`PIPELINE` | Runs another saved pipeline as a single step and returns its answer — a function call between pipelines, and the way to reuse a branch instead of copying it. | 1 | 1 | input forwarded as-is |
| **Skill**<br>`SKILL` | Runs a reusable skill: a fixed instruction plus the list of tools that skill may use. The allowlist is enforced when a tool is called, not merely suggested in the prompt. | 1 | 1 | originalTask, nodeInput |

† A port the node only has while its configuration asks for it — see that node's own section below.

\* `OUTPUT` — only when the node has a prompt of its own; in pass-through mode the upstream text is forwarded without any context being composed.
<!-- /AUTO-GEN:NODE_REFERENCE -->

## Node reference

<!-- AUTO-GEN:NODE_CONFIG -->
### Input — `INPUT`

Where the run starts: your message, a shared page, or the prompt a trigger carries. Exactly one per pipeline.

- **Ports.** No inbound port; one unlabelled outbound port.
- **Context on a new node.** This type ignores the context configuration — it forwards the upstream text unchanged.
- **System prompt.** A new node of this type starts without a system prompt.

| Field | Default | Reaches the run |
|---|---|---|
| `inputName` | `"user.message"` | **No** — stored and exported, but nothing reads it during a run (the entry contract is fixed; the run's text arrives as it is) |
| `schemaJson` | `null` | **No** — stored and exported, but nothing reads it during a run (typed pipeline inputs are not implemented) |

### Output — `OUTPUT`

Where the run ends and the answer reaches you. With no prompt of its own it forwards the previous node's text verbatim; give it one and it re-formats that text with one more model pass. Exactly one per pipeline.

- **Ports.** One inbound port; no outbound port.
- **Context on a new node.** `nodeInput` — only when the node has a prompt of its own; in pass-through mode the upstream text is forwarded without any context being composed.
- **System prompt.** A new node of this type starts without a system prompt.

| Field | Default | Reaches the run |
|---|---|---|
| `format` | `OutputFormat.PLAIN_TEXT` | **No** — stored and exported, but nothing reads it during a run (ask for the format in the prompt instead) |
| `systemPrompt` | `""` | **Yes** — saved as the node's `systemPrompt` |

### LiteRT — `LITE_RT`

One inference step on the model running on the phone. The default answering node, and the one to reach for unless a step genuinely needs a larger model.

- **Ports.** One inbound port; one unlabelled outbound port.
- **Context on a new node.** `originalTask`, `nodeInput`.
- **System prompt.** A new node of this type starts with the shipped default prompt for its type.

| Field | Default | Reaches the run |
|---|---|---|
| `modelId` | `""` | **Yes** — saved as the node's `modelPath` |
| `systemPrompt` | `""` | **Yes** — saved as the node's `systemPrompt` |
| `temperature` | `0.7f` | **No** — stored and exported, but nothing reads it during a run (the engine leaves the model's own sampler in place) |
| `topP` | `0.9f` | **No** — stored and exported, but nothing reads it during a run (the engine leaves the model's own sampler in place) |
| `maxNewTokens` | `512` | **No** — stored and exported, but nothing reads it during a run (the engine leaves the model's own sampler in place) |
| `stopTokens` | `emptyList()` | **No** — stored and exported, but nothing reads it during a run (the engine leaves the model's own sampler in place) |

### Cloud — `CLOUD`

One inference step against a configured cloud provider. Everything this node is given leaves the device, so it is a node to place deliberately rather than by default.

- **Ports.** One inbound port; one unlabelled outbound port.
- **Context on a new node.** `chatHistory`, `originalTask`, `nodeInput`.
- **System prompt.** A new node of this type starts with the shipped default prompt for its type.

| Field | Default | Reaches the run |
|---|---|---|
| `provider` | `CloudProvider.OPEN_AI` | **Yes** — saved as the node's `cloudProvider` |
| `model` | `""` | **No** — stored and exported, but nothing reads it during a run (the model comes from the provider's own setting) |
| `systemPrompt` | `""` | **Yes** — saved as the node's `systemPrompt` |
| `temperature` | `0.7f` | **No** — stored and exported, but nothing reads it during a run (the request carries no sampling parameters) |
| `maxTokens` | `1024` | **No** — stored and exported, but nothing reads it during a run (the request carries no sampling parameters) |
| `timeoutMs` | `30_000` | **No** — stored and exported, but nothing reads it during a run (cloud timeouts come from the client's own configuration) |

### Intent Router — `INTENT_ROUTER`

Sorts the incoming text into one of the classes you declare and sends the run down the matching branch — the way to give one pipeline several behaviours.

- **Ports.** One inbound port; one outbound port per class declared on the node.
- **Context on a new node.** `chatHistory`, `originalTask`, `nodeInput`.
- **System prompt.** A new node of this type starts with the shipped default prompt for its type.

| Field | Default | Reaches the run |
|---|---|---|
| `classes` | `emptyList()` | **Shapes the graph** — one outbound port per class; the run follows the edge the model picks |
| `classifierPrompt` | `""` | **No** — stored and exported, but nothing reads it during a run (the node keeps the prompt it was created with; see the note under this table) |
| `fallbackClass` | `null` | **No** — stored and exported, but nothing reads it during a run (an answer matching no class takes the node's first outgoing edge) |
| `engineProvider` | `null` | **Yes** — saved as the node's `cloudProvider` |

### If Condition — `IF_CONDITION`

A two-way branch: a yes/no question about the input, or a deterministic check for whether the run carries an image.

- **Ports.** One inbound port; outbound ports **True**, **False**.
- **Context on a new node.** This type ignores the context configuration — it forwards the upstream text unchanged.
- **System prompt.** A new node of this type starts without a system prompt.

| Field | Default | Reaches the run |
|---|---|---|
| `expression` | `""` | **Yes** — saved as the node's `conditionPrompt` |
| `labelTrue` | `"True"` | **No** — stored and exported, but nothing reads it during a run (the ports are always labelled True and False) |
| `labelFalse` | `"False"` | **No** — stored and exported, but nothing reads it during a run (the ports are always labelled True and False) |
| `branchOnImage` | `false` | **Yes** — saved as the node's `conditionHasImage` |
| `engineProvider` | `null` | **Yes** — saved as the node's `cloudProvider` |

### Clarification — `CLARIFICATION`

Pauses the run to ask you a question and waits for the answer, which then becomes the node's output. The one node that deliberately stops mid-run.

- **Ports.** One inbound port; one unlabelled outbound port.
- **Context on a new node.** `originalTask`, `nodeInput`.
- **System prompt.** A new node of this type starts with the shipped default prompt for its type.

| Field | Default | Reaches the run |
|---|---|---|
| `questionTemplate` | `""` | **Yes** — saved as the node's `systemPrompt` |
| `quickReplies` | `emptyList()` | **No** — stored and exported, but nothing reads it during a run (the answer options are produced by the model) |
| `timeoutMs` | `null` | **Yes** — saved as the node's `clarificationTimeoutMs` |

### Tool — `TOOL`

Calls one tool — a built-in, an AppFunction from another app, or one published by an MCP server — and passes the result on. Anything its risk level does not clear waits for your approval.

- **Ports.** One inbound port; one unlabelled outbound port.
- **Context on a new node.** `nodeInput`.
- **System prompt.** A new node of this type starts without a system prompt.

| Field | Default | Reaches the run |
|---|---|---|
| `toolId` | `""` | **Yes** — saved as the node's `toolName` |
| `argumentMapping` | `emptyList()` | **No** — stored and exported, but nothing reads it during a run (arguments are produced by the model from the node's input) |
| `confirmOverride` | `null` | **No** — stored and exported, but nothing reads it during a run (approval follows the tool's risk level and your settings) |
| `engineProvider` | `null` | **Yes** — saved as the node's `cloudProvider` |

### Decomposition — `DECOMPOSITION`

Turns one instruction into a list of subtasks. On its own it only produces the list; pair it with a Queue Processor to work through it.

- **Ports.** One inbound port; one unlabelled outbound port.
- **Context on a new node.** `originalTask`, `nodeInput`.
- **System prompt.** A new node of this type starts with the shipped default prompt for its type.

| Field | Default | Reaches the run |
|---|---|---|
| `planningPrompt` | `""` | **No** — stored and exported, but nothing reads it during a run (the node keeps the prompt it was created with; see the note under this table) |
| `maxSubtasks` | `5` | **No** — stored and exported, but nothing reads it during a run (how many subtasks appear is decided by the prompt and the run's step ceiling) |
| `outputSchemaJson` | `null` | **No** — stored and exported, but nothing reads it during a run (the subtask list is validated against a fixed shape) |
| `engineProvider` | `null` | **Yes** — saved as the node's `cloudProvider` |

### Queue Processor — `QUEUE_PROCESSOR`

Walks a list of subtasks one at a time, sending each down the Item branch and taking the Done branch when the list is empty. The only node that can send a run backwards.

- **Ports.** One inbound port; outbound ports **Item**, **Done**.
- **Context on a new node.** This type ignores the context configuration — it forwards the upstream text unchanged.
- **System prompt.** A new node of this type starts without a system prompt.

| Field | Default | Reaches the run |
|---|---|---|
| `inputList` | `""` | **No** — stored and exported, but nothing reads it during a run (the queue is the subtask list produced upstream) |
| `itemVariable` | `"item"` | **No** — stored and exported, but nothing reads it during a run (the current subtask arrives as the node's input) |
| `parallelism` | `1` | **No** — stored and exported, but nothing reads it during a run (subtasks always run one at a time) |
| `stopOnError` | `true` | **No** — stored and exported, but nothing reads it during a run (what happens after a failed subtask is decided by the graph, not by this switch) |

### Evaluation — `EVALUATION`

Judges what the previous step produced and answers Pass, Retry or Fail — the node that lets a pipeline have another go instead of handing you a bad answer.

- **Ports.** One inbound port; outbound ports **Pass**, **Retry** (conditional), **Fail**.
- **Context on a new node.** `originalTask`, `nodeInput`, `toolResults`.
- **System prompt.** A new node of this type starts with the shipped default prompt for its type.

| Field | Default | Reaches the run |
|---|---|---|
| `criteriaPrompt` | `""` | **No** — stored and exported, but nothing reads it during a run (the node keeps the prompt it was created with; see the note under this table) |
| `maxRetries` | `2` | **Shapes the graph** — above zero it gives the node a Retry port; it does not itself cap how often that branch is taken |
| `engineProvider` | `null` | **Yes** — saved as the node's `cloudProvider` |

### Summary — `SUMMARY`

Condenses what several earlier steps produced into one piece of text, typically just before the Output node at the end of a loop.

- **Ports.** One inbound port; one unlabelled outbound port.
- **Context on a new node.** `originalTask`, `nodeInput`, `toolResults`.
- **System prompt.** A new node of this type starts with the shipped default prompt for its type.

| Field | Default | Reaches the run |
|---|---|---|
| `format` | `SummaryFormat.BULLETS` | **No** — stored and exported, but nothing reads it during a run (ask for the shape in the prompt instead) |
| `customPrompt` | `null` | **No** — stored and exported, but nothing reads it during a run (the node keeps the prompt it was created with; see the note under this table) |
| `targetLengthChars` | `600` | **No** — stored and exported, but nothing reads it during a run (ask for the length in the prompt instead) |

### Pipeline — `PIPELINE`

Runs another saved pipeline as a single step and returns its answer — a function call between pipelines, and the way to reuse a branch instead of copying it.

- **Ports.** One inbound port; one unlabelled outbound port.
- **Context on a new node.** This type ignores the context configuration — it forwards the upstream text unchanged.
- **System prompt.** A new node of this type starts without a system prompt.

| Field | Default | Reaches the run |
|---|---|---|
| `targetPipelineId` | `""` | **Yes** — saved as the node's `targetPipelineId` |
| `targetPipelineName` | `null` | **No** — stored and exported, but nothing reads it during a run (shown in the picker; resolved from the id) |

### Skill — `SKILL`

Runs a reusable skill: a fixed instruction plus the list of tools that skill may use. The allowlist is enforced when a tool is called, not merely suggested in the prompt.

- **Ports.** One inbound port; one unlabelled outbound port.
- **Context on a new node.** `originalTask`, `nodeInput`.
- **System prompt.** A new node of this type starts without a system prompt.

| Field | Default | Reaches the run |
|---|---|---|
| `skillId` | `""` | **Yes** — saved as the node's `skillId` |
| `skillName` | `null` | **No** — stored and exported, but nothing reads it during a run (shown in the picker; resolved from the id) |
| `instructionPreview` | `null` | **No** — stored and exported, but nothing reads it during a run (read-only preview; edit the skill in the skill library) |
| `toolRestrictionSummary` | `null` | **No** — stored and exported, but nothing reads it during a run (read-only preview; edit the skill in the skill library) |
| `engine` | `SkillEngine.LITE_RT` | **Yes** — saved as the node's `cloudProvider` |

<!-- /AUTO-GEN:NODE_CONFIG -->

## Recipes

Each recipe below is a pipeline you can import and run. The JSON files live in
[docs/recipes/](recipes/) and are checked on every build: they go through the
same parser the app's import uses, and the graphs they produce through the same
validator the editor's validation bar runs. A recipe that stopped importing
would fail the build rather than fail you.

**To use one:** download the `.json` file, then in the app open **Pipelines →
Visual Orchestrator → Import JSON** and pick it. A bundle (the composition
recipe) is detected automatically and imports every pipeline it contains. Once
imported, a pipeline runs when you bind it to a chat and send a message — see
[Binding a pipeline to a chat](user-guide.md#binding-a-pipeline-to-a-chat).

The prompts are deliberately short. They are written for a small on-device
model, which follows a plain imperative sentence far better than a paragraph of
instructions.

### Routing on intent

[`recipes/intent-routing.json`](recipes/intent-routing.json)

```
Input → Router ─ Answer ─→ Answer ────────────────→ Output
               ├ Search ─→ Look it up → Answer from the result ─→ Output
               └ Ask ────→ Ask one question → Answer
```

One pipeline with three behaviours. The router classifies the message, and
each class has its own branch: answer directly, look the fact up with the
search tool first, or stop and ask the user one question when the message is
too vague to act on.

**What to change.** The classes. Each one needs a name, a description the
classifier can act on, and — this is the part that catches people — an outgoing
edge carrying exactly that name as its label. The class list draws the ports;
the run follows the edges. If you add a class and do not connect its port, the
run reaches the router and stops there.

**Put the class you would rather fall back to first.** When the model's answer
matches no class, the run takes the node's *first* outgoing edge, so the order
of connections is a real decision rather than a drawing detail. Here that is
`Answer`.

**Why the same message can take different branches.** The router sees the chat
history by default, so the same sentence genuinely is a different question in a
fresh chat than after ten turns. If you want the classification repeatable,
turn **Chat history** off under **Input Data** on the router — at the cost of
it no longer understanding follow-ups.

### A list of subtasks, worked through one at a time

[`recipes/decompose-and-queue.json`](recipes/decompose-and-queue.json)

```
Input → Plan → Each subtask ─ Item ─→ Do the subtask ─┐
                    ↑                                  │
                    └──────────────────────────────────┘
               Each subtask ─ Done ─→ Put it together → Output
```

The Decomposition node turns the request into a list; the Queue Processor walks
it. The **Item** branch runs once per subtask, and the edge from the end of
that branch back to the Queue Processor is what makes it a loop — it is the one
edge in the whole format allowed to point backwards. When the list is empty the
**Done** branch fires once.

**What to change.** The body of the loop. Here it is a single on-device model
step; it can be a tool call, a chain, or a whole sub-pipeline. Whatever it is,
its last node must connect back to the Queue Processor, or the run ends after
the first subtask.

**Watch the length.** Every subtask is a step, and a run has a step ceiling —
a plan of twenty subtasks will hit it. Keep the planning prompt asking for a
short list.

### A tool call you approve

[`recipes/tool-with-approval.json`](recipes/tool-with-approval.json)

```
Input → Draft the note → Save it (write_file) → Confirm
```

The model writes the text, the Tool node writes the file. `write_file` is not a
read-only tool, so by default the run pauses and asks you before it happens —
the approval card appears in the chat and the run resumes on your answer. What
is asked about is governed by the approval settings; the risk level is the
tool's own. This is the whole
point of the shape: the model decides *what*, you decide *whether*.

**What to change.** The tool. Every tool declares a risk level, and that is
what decides whether you are asked; you can also ask to be prompted for a tool
that would not otherwise stop. See
[Approving a tool call](user-guide.md#approving-a-tool-call).

**The Tool node does not take typed arguments from the sheet.** It is given the
node's input and the model produces the call from it, so the node before a Tool
node should produce exactly the text the tool needs — nothing else. That is why
the drafting node here is told to reply with the note text only.

### A run that uses long-term memory

[`recipes/memory-aware-run.json`](recipes/memory-aware-run.json)

```
Input → Answer with memory → Output
```

Three nodes, and the whole recipe is one checkbox and one line of JSON. The
model node has **Long-term memory** enabled under Input Data, so relevant
remembered entries are retrieved and given to it along with the question.

**The line of JSON is `memoryRetrievalQuery`,** declared at pipeline level. It
matters for pipelines that run in the background — from a trigger, a schedule
or the Quick Settings tile. Those runs carry a fixed authored prompt rather
than something you just typed, and a fixed prompt is a poor search key for
memory. The declared query replaces it. Write it as the *topic to recall*, not
as an instruction:

```json
"memoryRetrievalQuery": "commitments, plans and preferences the user mentioned recently, around $DATE"
```

Omit the key for pipelines that only ever run from a chat.

**What to change.** Whether memory is on at all, and where. Turning it on for
every node is the reflex to resist: retrieval happens for each node that asks
for it, and a router or a tool node rarely answers better for having your
history in its prompt.

### One pipeline calling another

[`recipes/composition-bundle.json`](recipes/composition-bundle.json)

```
Parent:  Input → Answer → Tidy up (Pipeline node) → Output
Sub:                      Input → Tidy → Tidied
```

A Pipeline node runs another saved pipeline as one step: its input becomes that
pipeline's message, and that pipeline's answer becomes the node's output. Reach
for it when a sub-flow is used in more than one place, or when a process has
phases you would rather look at separately.

This recipe is a **bundle** — one file carrying the parent and every pipeline
it calls — because a parent whose target is missing imports as a dangling
reference. The app detects the envelope and imports both.

**What to change.** The sub-pipeline. It is an ordinary pipeline: open it,
edit it, and every parent that calls it gets the change. That is the reuse the
node is for.

**The limits are checked before a run starts.** A pipeline cannot call itself,
directly or through a chain, and compositions can only nest so deep — the
target picker greys out any choice that would break either rule and says which
pipeline is the cause.

### What goes wrong most often

- **A node with every context block turned off.** It would receive an empty
  input, so the editor refuses to save until you enable one. The fix is
  usually `nodeInput`.
- **A router class with no edge.** The class draws a port; nothing connects it.
  The run reaches the router and has nowhere to go.
- **A queue with no edge back to it.** The first subtask runs, the loop never
  closes, and the Done branch never fires.
- **A Pipeline node with no target chosen.** A validation error, exactly like a
  dangling connection — the editor will not save it.
- **An Output node given a prompt by accident.** With a prompt it runs one more
  model pass over the answer instead of showing it, which reads as the model
  paraphrasing itself. Leave it empty unless you want a formatter.
- **A Cloud node placed out of habit.** Everything that node is given leaves
  the device. Use it where the task needs it and keep the rest on-device —
  which is also the faster arrangement more often than people expect.

## See also

- [User guide](user-guide.md) — the screens, and how the app behaves.
- [FAQ](faq.md) — what exists, what does not, and why.
- [Troubleshooting](troubleshooting.md) — when something is broken.
- [Extending the agent](extending.md) — adding a node type, a tool, a cloud
  provider or a prompt variable in Kotlin.
- [Architecture](architecture.md) — how a run is executed.
