<?xml version="1.0" encoding="utf-8"?>
<!--
    Russian (ru) localization placeholder.

    Phase 18/3 (UI strings → resources) lands the English baseline only;
    every user-facing string lives under `res/values/strings_*.xml`. This
    file marks the entry point for the first translation. To translate:

      1. Mirror entries from `res/values/strings.xml` and per-feature
         `res/values/strings_<feature>.xml` files here (and create the
         matching `res/values-ru/strings_<feature>.xml` siblings).
      2. Translate the `<string>` bodies; preserve the keys verbatim and
         the positional argument markers (`%1$s`, `%1$d`).
      3. Add `defaultConfig.resourceConfigurations += setOf("en", "ru")`
         to `app/build.gradle.kts` once the translations are reviewed.

    Empty file by design — Phase 18/3 ships English-only. Removing this
    file later is fine; keeping it now documents the intended path.
-->
<resources>
</resources>
