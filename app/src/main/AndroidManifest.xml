<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.EXECUTE_APP_FUNCTIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

<!--    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />-->

<!--    <queries>-->
<!--        <package android:name="com.samsung.android.dialer" />-->
<!--        <package android:name="com.sec.android.gallery3d" />-->
<!--        <package android:name="com.sec.android.app.myfiles" />-->
<!--        <package android:name="com.samsung.android.incallui" />-->
<!--        <package android:name="com.samsung.android.app.sharelive" />-->
<!--    </queries>-->

    <!--
        Phase 20 / Task 6/7: the agent must be able to see the :tools-probe package via
        `PackageManager.canPackageQuery` so `AppFunctionManager.observeAppFunctions`
        surfaces the probe's `echo` AppFunction during the e2e instrumented test.
        Android 11+ ships with package visibility on by default; without an explicit
        `<queries>` entry the system filters the probe out even though both APKs are
        installed on the same device.
    -->
    <queries>
        <package android:name="ai.agent.android.toolsprobe" />
    </queries>

    <application
        android:name=".App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:largeHeap="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/Theme.AndroidAIAgent">

        <!-- Firebase Crashlytics / Analytics — auto-collection is disabled
             by default to enforce the opt-in flow. The runtime
             `CrashReportingRepository.setEnabled(true)` flips this on once
             the user accepts the in-app consent dialog; the setting is
             persisted by Firebase across launches until cleared. -->
        <meta-data
            android:name="firebase_crashlytics_collection_enabled"
            android:value="false" />
        <meta-data
            android:name="firebase_analytics_collection_enabled"
            android:value="false" />

        <!-- Required for LiteRT GPU (OpenCL) acceleration -->
        <uses-native-library android:name="libOpenCL.so" android:required="false" />
        <uses-native-library android:name="libvndksupport.so" android:required="false" />
        <!-- Required for LiteRT NPU (Qualcomm DSP) acceleration -->
        <uses-native-library android:name="libcdsprpc.so" android:required="false" />

        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>
        <activity
            android:name=".presentation.ui.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.AndroidAIAgent">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".data.tools.local.AgentAppFunctionService"
            android:exported="true"
            android:permission="android.permission.BIND_APP_FUNCTION_SERVICE">
            <intent-filter>
                <action android:name="android.app.appfunctions.AppFunctionService" />
            </intent-filter>
        </service>

        <service
            android:name=".data.services.AgentForegroundService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="On-Device LLM Inference and Agent Orchestration" />
        </service>

        <receiver
            android:name=".presentation.receivers.AgentApprovalReceiver"
            android:exported="false" />
    </application>

</manifest>