package app.knotwork.design.icons

import androidx.compose.ui.graphics.vector.ImageVector
import app.knotwork.design.icons.imagevector.knotworkAddIcon
import app.knotwork.design.icons.imagevector.knotworkAlertCircleIcon
import app.knotwork.design.icons.imagevector.knotworkArchiveIcon
import app.knotwork.design.icons.imagevector.knotworkArrowDownIcon
import app.knotwork.design.icons.imagevector.knotworkArrowRIcon
import app.knotwork.design.icons.imagevector.knotworkArrowUpIcon
import app.knotwork.design.icons.imagevector.knotworkArrowUpLineIcon
import app.knotwork.design.icons.imagevector.knotworkAutoLayoutIcon
import app.knotwork.design.icons.imagevector.knotworkBackIcon
import app.knotwork.design.icons.imagevector.knotworkBatteryIcon
import app.knotwork.design.icons.imagevector.knotworkBlockIcon
import app.knotwork.design.icons.imagevector.knotworkBoltIcon
import app.knotwork.design.icons.imagevector.knotworkBookIcon
import app.knotwork.design.icons.imagevector.knotworkBookmarkAddIcon
import app.knotwork.design.icons.imagevector.knotworkBookmarkIcon
import app.knotwork.design.icons.imagevector.knotworkBrainIcon
import app.knotwork.design.icons.imagevector.knotworkBranchIcon
import app.knotwork.design.icons.imagevector.knotworkChatActiveIcon
import app.knotwork.design.icons.imagevector.knotworkChatIcon
import app.knotwork.design.icons.imagevector.knotworkCheckIcon
import app.knotwork.design.icons.imagevector.knotworkChipIcon
import app.knotwork.design.icons.imagevector.knotworkCircleIcon
import app.knotwork.design.icons.imagevector.knotworkCloudIcon
import app.knotwork.design.icons.imagevector.knotworkCogIcon
import app.knotwork.design.icons.imagevector.knotworkCopyIcon
import app.knotwork.design.icons.imagevector.knotworkDbIcon
import app.knotwork.design.icons.imagevector.knotworkDotIcon
import app.knotwork.design.icons.imagevector.knotworkDownload2Icon
import app.knotwork.design.icons.imagevector.knotworkDownloadIcon
import app.knotwork.design.icons.imagevector.knotworkEditIcon
import app.knotwork.design.icons.imagevector.knotworkExpandIcon
import app.knotwork.design.icons.imagevector.knotworkExtensionIcon
import app.knotwork.design.icons.imagevector.knotworkExternalIcon
import app.knotwork.design.icons.imagevector.knotworkEyeIcon
import app.knotwork.design.icons.imagevector.knotworkEyeOffIcon
import app.knotwork.design.icons.imagevector.knotworkFilterIcon
import app.knotwork.design.icons.imagevector.knotworkFlowActiveIcon
import app.knotwork.design.icons.imagevector.knotworkFlowIcon
import app.knotwork.design.icons.imagevector.knotworkGlobeIcon
import app.knotwork.design.icons.imagevector.knotworkGridIcon
import app.knotwork.design.icons.imagevector.knotworkGridOffIcon
import app.knotwork.design.icons.imagevector.knotworkHistoryIcon
import app.knotwork.design.icons.imagevector.knotworkHourglassIcon
import app.knotwork.design.icons.imagevector.knotworkHubIcon
import app.knotwork.design.icons.imagevector.knotworkInfoIcon
import app.knotwork.design.icons.imagevector.knotworkKeyIcon
import app.knotwork.design.icons.imagevector.knotworkLinkIcon
import app.knotwork.design.icons.imagevector.knotworkLockIcon
import app.knotwork.design.icons.imagevector.knotworkMarkIcon
import app.knotwork.design.icons.imagevector.knotworkMenuIcon
import app.knotwork.design.icons.imagevector.knotworkMicIcon
import app.knotwork.design.icons.imagevector.knotworkMinusCircleIcon
import app.knotwork.design.icons.imagevector.knotworkMinusIcon
import app.knotwork.design.icons.imagevector.knotworkMonitorIcon
import app.knotwork.design.icons.imagevector.knotworkMore2ActiveIcon
import app.knotwork.design.icons.imagevector.knotworkMore2Icon
import app.knotwork.design.icons.imagevector.knotworkMoreIcon
import app.knotwork.design.icons.imagevector.knotworkNodeBranchIcon
import app.knotwork.design.icons.imagevector.knotworkNodeClarifyIcon
import app.knotwork.design.icons.imagevector.knotworkNodeCloudIcon
import app.knotwork.design.icons.imagevector.knotworkNodeDecomposeIcon
import app.knotwork.design.icons.imagevector.knotworkNodeEvalIcon
import app.knotwork.design.icons.imagevector.knotworkNodeInputIcon
import app.knotwork.design.icons.imagevector.knotworkNodeIntentRouterIcon
import app.knotwork.design.icons.imagevector.knotworkNodeLiteIcon
import app.knotwork.design.icons.imagevector.knotworkNodeOutputIcon
import app.knotwork.design.icons.imagevector.knotworkNodeQueueIcon
import app.knotwork.design.icons.imagevector.knotworkNodeSummaryIcon
import app.knotwork.design.icons.imagevector.knotworkNodeToolIcon
import app.knotwork.design.icons.imagevector.knotworkPasteIcon
import app.knotwork.design.icons.imagevector.knotworkPauseIcon
import app.knotwork.design.icons.imagevector.knotworkPinIcon
import app.knotwork.design.icons.imagevector.knotworkPinOnIcon
import app.knotwork.design.icons.imagevector.knotworkPlayIcon
import app.knotwork.design.icons.imagevector.knotworkRamIcon
import app.knotwork.design.icons.imagevector.knotworkRedoIcon
import app.knotwork.design.icons.imagevector.knotworkRefreshIcon
import app.knotwork.design.icons.imagevector.knotworkSaveIcon
import app.knotwork.design.icons.imagevector.knotworkSearchIcon
import app.knotwork.design.icons.imagevector.knotworkSendIcon
import app.knotwork.design.icons.imagevector.knotworkShieldIcon
import app.knotwork.design.icons.imagevector.knotworkSlidersIcon
import app.knotwork.design.icons.imagevector.knotworkSparkIcon
import app.knotwork.design.icons.imagevector.knotworkStarIcon
import app.knotwork.design.icons.imagevector.knotworkStopIcon
import app.knotwork.design.icons.imagevector.knotworkTerminalIcon
import app.knotwork.design.icons.imagevector.knotworkThemeIcon
import app.knotwork.design.icons.imagevector.knotworkToolActiveIcon
import app.knotwork.design.icons.imagevector.knotworkToolIcon
import app.knotwork.design.icons.imagevector.knotworkTrashIcon
import app.knotwork.design.icons.imagevector.knotworkUndoIcon
import app.knotwork.design.icons.imagevector.knotworkWarnIcon
import app.knotwork.design.icons.imagevector.knotworkWordmarkIcon
import app.knotwork.design.icons.imagevector.knotworkXIcon

/**
 * Knotwork icon facade.
 *
 * Holds the custom single-stroke icon family: the brand marks, the 12 pipeline
 * node glyphs, and the `I.*` general-UI set (`menu`, `back`, `search`,
 * `add`, `edit`, `trash`, …) built from the source SVG set.
 * Prefer these over Material Icons so the UI reads as one stroke family; a
 * handful of low-frequency glyphs with no `I.*` counterpart still use Material
 * Icons directly.
 *
 * Implementations live in [imagevector] (one file per icon). Each file
 * exposes a top-level `internal val knotwork<Name>Icon: ImageVector`
 * lazily built from SVG path data, and the facade delegates to it. The
 * `*Active` variants restroke the bottom-nav glyphs at 2.0 for the selected
 * tab.
 */
object AppIcons {
    /** Knotwork wordmark glyph (used in compact contexts; the literal "Knotwork" text wordmark is rendered with Inter). */
    val Wordmark: ImageVector get() = knotworkWordmarkIcon

    /** Knotwork brand mark (the in-app 24×24 simplification of the launcher icon). */
    val Mark: ImageVector get() = knotworkMarkIcon

    /** "Pipelines" bottom-nav tab — three connected nodes. */
    val Flow: ImageVector get() = knotworkFlowIcon

    /** Pipeline `INPUT` node glyph. */
    val NodeInput: ImageVector get() = knotworkNodeInputIcon

    /** Pipeline `INTENT_ROUTER` node glyph. */
    val NodeIntentRouter: ImageVector get() = knotworkNodeIntentRouterIcon

    /** Pipeline `IF_CONDITION` node glyph. */
    val NodeBranch: ImageVector get() = knotworkNodeBranchIcon

    /** Pipeline `CLARIFICATION` node glyph. */
    val NodeClarify: ImageVector get() = knotworkNodeClarifyIcon

    /** Pipeline `LITE_RT` node glyph (local on-device LLM). */
    val NodeLite: ImageVector get() = knotworkNodeLiteIcon

    /** Pipeline `CLOUD` node glyph. */
    val NodeCloud: ImageVector get() = knotworkNodeCloudIcon

    /** Pipeline `TOOL` node glyph. */
    val NodeTool: ImageVector get() = knotworkNodeToolIcon

    /** Pipeline `DECOMPOSITION` node glyph. */
    val NodeDecompose: ImageVector get() = knotworkNodeDecomposeIcon

    /** Pipeline `QUEUE_PROCESSOR` node glyph. */
    val NodeQueue: ImageVector get() = knotworkNodeQueueIcon

    /** Pipeline `EVALUATION` node glyph. */
    val NodeEval: ImageVector get() = knotworkNodeEvalIcon

    /** Pipeline `SUMMARY` node glyph. */
    val NodeSummary: ImageVector get() = knotworkNodeSummaryIcon

    /** Pipeline `OUTPUT` node glyph. */
    val NodeOutput: ImageVector get() = knotworkNodeOutputIcon

    /** Editor toolbar — auto-layout action. */
    val AutoLayout: ImageVector get() = knotworkAutoLayoutIcon

    /** Memory entry / "Memory" navigation glyph. */
    val Brain: ImageVector get() = knotworkBrainIcon

    // ── Active (2.0 stroke) bottom-nav variants ───────────────────────────
    /** `I.chat` at active 2.0 stroke — selected Chat tab. */
    val ChatActive: ImageVector get() = knotworkChatActiveIcon

    /** `I.flow` at active 2.0 stroke — selected Pipelines tab. */
    val FlowActive: ImageVector get() = knotworkFlowActiveIcon

    /** `I.tool` at active 2.0 stroke — selected Tools tab. */
    val ToolActive: ImageVector get() = knotworkToolActiveIcon

    /** `I.more2` at active 2.0 stroke — selected More tab. */
    val More2Active: ImageVector get() = knotworkMore2ActiveIcon

    // ── General UI icon family (`I.*`) ────────────────────────────────────
    /** `I.menu` — hamburger / drawer. */
    val Menu: ImageVector get() = knotworkMenuIcon

    /** `I.back` — back. */
    val Back: ImageVector get() = knotworkBackIcon

    /** `I.more` — overflow ⋮ (solid). */
    val More: ImageVector get() = knotworkMoreIcon

    /** `I.more2` — overflow … (horizontal). */
    val More2: ImageVector get() = knotworkMore2Icon

    /** `I.chat` — chat tab. */
    val Chat: ImageVector get() = knotworkChatIcon

    /** `I.tool` — tool. */
    val Tool: ImageVector get() = knotworkToolIcon

    /** `I.send` — send message. */
    val Send: ImageVector get() = knotworkSendIcon

    /** `I.add` — add / FAB +. */
    val Add: ImageVector get() = knotworkAddIcon

    /** `I.search` — search. */
    val Search: ImageVector get() = knotworkSearchIcon

    /** `I.edit` — edit / rename. */
    val Edit: ImageVector get() = knotworkEditIcon

    /** `I.star` — favorite / pin-list. */
    val Star: ImageVector get() = knotworkStarIcon

    /** `I.filter` — filter / facets. */
    val Filter: ImageVector get() = knotworkFilterIcon

    /** `I.check` — confirm / tick. */
    val Check: ImageVector get() = knotworkCheckIcon

    /** `I.x` — close / clear. */
    val X: ImageVector get() = knotworkXIcon

    /** `I.shield` — privacy / security. */
    val Shield: ImageVector get() = knotworkShieldIcon

    /** `I.cog` — settings. */
    val Cog: ImageVector get() = knotworkCogIcon

    /** `I.download` — download. */
    val Download: ImageVector get() = knotworkDownloadIcon

    /** `I.download2` — import / export (alt). */
    val Download2: ImageVector get() = knotworkDownload2Icon

    /** `I.play` — run (solid). */
    val Play: ImageVector get() = knotworkPlayIcon

    /** `I.pause` — pause (solid). */
    val Pause: ImageVector get() = knotworkPauseIcon

    /** `I.spark` — auto / AI action. */
    val Spark: ImageVector get() = knotworkSparkIcon

    /** `I.link` — link / connect. */
    val Link: ImageVector get() = knotworkLinkIcon

    /** `I.db` — storage / DB. */
    val Db: ImageVector get() = knotworkDbIcon

    /** `I.monitor` — monitoring / metrics. */
    val Monitor: ImageVector get() = knotworkMonitorIcon

    /** `I.history` — history. */
    val History: ImageVector get() = knotworkHistoryIcon

    /** `I.bolt` — quick action / energy. */
    val Bolt: ImageVector get() = knotworkBoltIcon

    /** `I.trash` — delete. */
    val Trash: ImageVector get() = knotworkTrashIcon

    /** `I.copy` — copy. */
    val Copy: ImageVector get() = knotworkCopyIcon

    /** `I.dot` — marker dot (solid). */
    val Dot: ImageVector get() = knotworkDotIcon

    /** `I.arrowR` — forward / expand →. */
    val ArrowR: ImageVector get() = knotworkArrowRIcon

    /** `I.arrowDown` — expand ▽. */
    val ArrowDown: ImageVector get() = knotworkArrowDownIcon

    /** `I.warn` — warning. */
    val Warn: ImageVector get() = knotworkWarnIcon

    /** `I.lock` — lock. */
    val Lock: ImageVector get() = knotworkLockIcon

    /** `I.key` — API key. */
    val Key: ImageVector get() = knotworkKeyIcon

    /** `I.cloud` — cloud LLM. */
    val Cloud: ImageVector get() = knotworkCloudIcon

    /** `I.chip` — on-device / NPU. */
    val Chip: ImageVector get() = knotworkChipIcon

    /** `I.globe` — network / locale. */
    val Globe: ImageVector get() = knotworkGlobeIcon

    /** `I.branch` — branch / IF. */
    val Branch: ImageVector get() = knotworkBranchIcon

    /** `I.pin` — pin (outline). */
    val Pin: ImageVector get() = knotworkPinIcon

    /** `I.pin` in its pin-on (solid-filled) state. */
    val PinOn: ImageVector get() = knotworkPinOnIcon

    // ── General UI icon family — additional glyphs ────────────────────────
    /** `I.refresh` — retry / refresh. */
    val Refresh: ImageVector get() = knotworkRefreshIcon

    /** `I.bookmark` — saved / bookmark. */
    val Bookmark: ImageVector get() = knotworkBookmarkIcon

    /** `I.bookmarkAdd` — add to bookmarks. */
    val BookmarkAdd: ImageVector get() = knotworkBookmarkAddIcon

    /** `I.arrowUp` — collapse ▲ (chevron, pairs arrowDown). */
    val ArrowUp: ImageVector get() = knotworkArrowUpIcon

    /** `I.arrowUpLine` — up / scroll-to-top (straight). */
    val ArrowUpLine: ImageVector get() = knotworkArrowUpLineIcon

    /** `I.terminal` — console >_. */
    val Terminal: ImageVector get() = knotworkTerminalIcon

    /** `I.alertCircle` — error/info in circle (≠ warn triangle). */
    val AlertCircle: ImageVector get() = knotworkAlertCircleIcon

    /** `I.info` — info (i). */
    val Info: ImageVector get() = knotworkInfoIcon

    /** `I.hub` — hub / integrations. */
    val Hub: ImageVector get() = knotworkHubIcon

    /** `I.eye` — show password. */
    val Eye: ImageVector get() = knotworkEyeIcon

    /** `I.eyeOff` — hide password. */
    val EyeOff: ImageVector get() = knotworkEyeOffIcon

    /** `I.stop` — stop run (solid square). */
    val Stop: ImageVector get() = knotworkStopIcon

    /** `I.block` — blocked. */
    val Block: ImageVector get() = knotworkBlockIcon

    /** `I.theme` — theme toggle (half-filled circle). */
    val Theme: ImageVector get() = knotworkThemeIcon

    /** `I.external` — open external link. */
    val External: ImageVector get() = knotworkExternalIcon

    /** `I.expand` — fullscreen / open in full. */
    val Expand: ImageVector get() = knotworkExpandIcon

    /** `I.battery` — battery alert. */
    val Battery: ImageVector get() = knotworkBatteryIcon

    /** `I.hourglass` — pending / queued. */
    val Hourglass: ImageVector get() = knotworkHourglassIcon

    /** `I.mic` — voice input. */
    val Mic: ImageVector get() = knotworkMicIcon

    /** `I.archive` — archive. */
    val Archive: ImageVector get() = knotworkArchiveIcon

    /** `I.book` — documentation. */
    val Book: ImageVector get() = knotworkBookIcon

    /** `I.minus` — minus / zoom-out. */
    val Minus: ImageVector get() = knotworkMinusIcon

    /** `I.minusCircle` — remove item. */
    val MinusCircle: ImageVector get() = knotworkMinusCircleIcon

    /** `I.extension` — plugin / extension. */
    val Extension: ImageVector get() = knotworkExtensionIcon

    /** `I.save` — save. */
    val Save: ImageVector get() = knotworkSaveIcon

    /** `I.undo` — undo. */
    val Undo: ImageVector get() = knotworkUndoIcon

    /** `I.redo` — redo. */
    val Redo: ImageVector get() = knotworkRedoIcon

    /** `I.grid` — snap grid on. */
    val Grid: ImageVector get() = knotworkGridIcon

    /** `I.gridOff` — snap grid off. */
    val GridOff: ImageVector get() = knotworkGridOffIcon

    /** `I.paste` — paste (clipboard). */
    val Paste: ImageVector get() = knotworkPasteIcon

    /** `I.circle` — empty state dot (radio unchecked). */
    val Circle: ImageVector get() = knotworkCircleIcon

    /** `I.sliders` — tune (≠ filter funnel). */
    val Sliders: ImageVector get() = knotworkSlidersIcon

    /** `I.ram` — memory / RAM (settings). */
    val Ram: ImageVector get() = knotworkRamIcon
}
