# Consumer ProGuard rules contributed by the :catalog design-system module.
# Intentionally empty: the module exposes only Compose
# composables and design tokens, both of which are kept by R8 through Compose's
# own consumer rules shipped with `androidx.compose.runtime`.
